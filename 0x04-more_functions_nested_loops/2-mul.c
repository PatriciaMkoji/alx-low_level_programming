#include <stdio.h>
#include "main.h"

/**
 * mul - a function that multiplies 2 digits
 * @a :to be multiplied
 * @b :to bemuliplied
 * Return: 0;
 */

int mul(int a, int b)
{
	int  mul;
	
	mul = (a * b);

	return (mul);
}
