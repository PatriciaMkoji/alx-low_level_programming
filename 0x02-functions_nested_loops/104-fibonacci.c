#include <stdio.h>
#include "main.h"

/**
 * print_to_98 - print all nubers from inpur to 98
 * @n: the starting number
 */
void print_to_98(intn)
{
	i(n >= 98)
	{
		while(n > 98)
			printf("%d\n", n);
	}
	else
	{
		while(n <98)
			printf("%d, ", n++);
		printf("%d\n", n);
	}
}
